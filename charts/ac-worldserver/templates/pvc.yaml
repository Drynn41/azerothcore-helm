apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.worldserver.pvc.pvc_name }}
  namespace: {{ .Values.namespace }}  # Utilisation du namespace spécifié dans values.yaml
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.worldserver.pvc.size }}
  storageClassName: {{ .Values.worldserver.pvc.storageClass }}

