apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.ac-db.service.name }}
  labels:
    app: {{ .Values.ac-db.name }}
spec:
  clusterIP: None  # Headless service
  ports:
    - port: {{ .Values.ac-db.service.port }}  # Port de la base de données
      targetPort: {{ .Values.ac-db.service.targetPort }}
  selector:
    app: {{ .Values.ac-db.name }}

