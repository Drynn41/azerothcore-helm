apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ac-db.fullname" . }}-test-connection"
  labels:
    {{- include "ac-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ac-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
