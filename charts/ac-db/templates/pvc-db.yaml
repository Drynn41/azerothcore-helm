apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.ac-db.pvc.pvc_name }}
  namespace: {{ .Values.namespace }}  # Utilisation du namespace spécifié dans values.yaml
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.ac-db.persistence.size }}
  storageClassName: {{ .Values.ac-db.persistence.storageClass }}

