apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.cms.pvc.pvc_name }}
  namespace: {{ .Values.namespace }}  # Utilisation du namespace spécifié dans values.yaml
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.cms.persistence.size }}
  storageClassName: {{ .Values.cms.persistence.storageClass }}

