apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ac-client-data-init.fullname" . }}-test-connection"
  labels:
    {{- include "ac-client-data-init.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ac-client-data-init.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
